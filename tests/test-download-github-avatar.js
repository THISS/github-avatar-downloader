const assert = require("assert");

describe("process.env",() => {
  describe("GITHUB_API and USER should have valid keys", () => {
    it("should return true when no value is present", () => {
      assert.equal(true,)
    });
  });
});